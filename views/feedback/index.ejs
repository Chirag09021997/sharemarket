<!DOCTYPE html>
<html lang="en">

<head>
  <%- include('../partials/head', { title: title }) %>
    <link href="https://cdn.datatables.net/1.11.3/css/jquery.dataTables.min.css" rel="stylesheet" />
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>
    <script>
      $(document).ready(function () {
        $("#indexData").DataTable({
          pageLength: 25,
          lengthMenu: [10, 25, 50, 100],
          searching: true,
          ordering: true,
          order: [[1, 'asc']],
        });
      });
    </script>
</head>

<body>
  <%- include('../partials/navbar') %>
    <%- include('../partials/sidebar') %>
      <div class="sm:p-4 p-1 sm:ml-64 mt-6">
        <section class="p-3 sm:p-5">
          <div class="bg-white shadow-md sm:rounded-lg mx-auto p-4 my-5">
            <div class="flex justify-between items-center">
              <h1 class="text-xl font-bold">List FeedBack</h1>
            </div>
          </div>

          <div class="bg-white shadow-md sm:rounded-lg mx-auto p-4 my-5 relative overflow-x-auto">
            <table id="indexData" class="display w-full text-sm text-gray-500">
              <thead class="text-xs text-gray-700 uppercase bg-gray-50">
                <tr>
                  <th>Action</th>
                  <th>#</th>
                  <th>Device Id</th>
                  <th>Title</th>
                  <th>Image</th>
                  <th>Description</th>
                </tr>
              </thead>
              <tbody>
                <% if (getData && getData.length> 0) { %>
                  <% getData.forEach((data, index)=> { %>
                    <tr key="<%= index %>">
                      <td class="flex justify-center">
                        <button data-id="<%= data.id %>" data-url="feedback" class="mx-2 deleteRecord text-red-500">
                          <i class="fas fa-trash-alt"></i>
                        </button>
                      </td>
                      <td class="text-center">
                        <%= index + 1 %>
                      </td>
                      <td class="text-center">
                        <%= data.device_id %>
                      </td>
                      <td class="text-center">
                        <%= data.title %>
                      </td>
                      <td class="text-center">
                        <img src="<%= data.image %>" alt="img" class="w-14 h-14">
                      </td>
                      <td class="text-center">
                        <%= data.description %>
                      </td>
                    </tr>
                    <% }) %>
                      <% } else { %>
                        <tr>
                          <td colspan="5" class="text-center">No data available</td>
                        </tr>
                        <% } %>
              </tbody>
            </table>
        </section>
      </div>
</body>

</html>